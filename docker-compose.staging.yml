services:
  drivstoffpriser-staging:
    build: .
    restart: always
    ports:
      - "3004:3004"
    environment:
      PORT: "3004"
      DB_PATH: "/app/data/drivstoff.db"
      SECRET_KEY: "staging-nøkkel"
      STATS_KEY: "staging"
      BASE_URL: "http://100.76.35.106:3004"
      DATA_DIR: "/app/data"
      OCR_MODELL: "${OCR_MODELL:-gemini}"
      GEMINI_API_KEY: "${GEMINI_API_KEY:-}"
      GEMINI_MODELL: "${GEMINI_MODELL:-gemini-flash-latest}"
      GEMINI_MODELLER: "${GEMINI_MODELLER:-gemini-flash-latest,gemini-2.5-flash}"
      LOG_LEVEL: "DEBUG"
    volumes:
      - drivstoff-staging-data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"

volumes:
  drivstoff-staging-data:
